# 安全策略

请不要在公开 Issue 中粘贴真实文件路径、用户名、索引数据库、凭据或其他隐私数据。

发现安全问题时，请通过 GitHub Security Advisory 的 **Report a vulnerability** 私下报告，并附最小复现、影响范围和建议修复。维护者会先确认收到，再根据风险协调修复和披露。

当前安全边界：服务仅监听本机 Windows Named Pipe；MCP 工具只返回索引元数据，不读取文件正文，也不提供执行、修改或删除文件的工具。
